
<%= form_for(@department) do |f| %>
  <% if @department.errors.any? %>
    <div id="error_explanation">
      <h2 class="label-warning"><%= pluralize(@department.errors.count, "error") %> prohibited this department from being saved:</h2>

      <ul class="nav">
      <% @department.errors.full_messages.each do |msg| %>
        <li class="label-info"><%= msg%></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
